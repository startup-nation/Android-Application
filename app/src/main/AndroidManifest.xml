<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookmymeal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookMyTable">
        <activity android:name=".RestaurantRequestListActivity"></activity>
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".CustomerDashboardActivity" />
        <activity android:name=".RestaurantAddOffersActivity" />
        <activity android:name=".RestaurantOffersActivity" />
        <activity android:name=".RestaurantAddFoodsActivity" />
        <activity android:name=".RestaurantFoodsActivity" />
        <activity android:name=".RestaurantProfileActivity" />
        <activity android:name=".RestaurantOwnerActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>